query fetchAutostoreMessage($maxRetries: Int) {
  fetchAutostoreMessage(MaxRetries: $maxRetries) {
    _id
    Type
    TypeID
    Action
    Endpoint
    Status
    Timestamp
    Message
    ErrorCount
  }
}