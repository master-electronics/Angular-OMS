query fetchMenuList($pageName: String) {
    fetchMenuList(pageName: $pageName) {
        _id
        ParentID
        Route
        RouteID
        Front
        Highlight
        End
        CoverSrc
        ADGroupProtected
        Order
        Level
        Groups
        Title
    }
}